{% extends 'base.html.twig' %}

{% block title %}New product{% endblock %}

{% block body %}
<div class="container page-add">
    <h2>Add product</h2>
    {{ form_start(AddProduct) }}
        {{ form_row(AddProduct.name) }}
        <br>
        {{ form_row(AddProduct.price) }}
        <br>
        {{ form_row(AddProduct.description) }}
        <br>
        {{ form_row(AddProduct.category) }}
        <br>
        {{ form_row(AddProduct.img) }}
        <br>
        <button class="btn btn-success"><i class="fas fa-plus"></i> Add</button>
    {{ form_end(AddProduct) }}
    <br><br>
    {% if SuccessMsg != '' %}
        <div class="alert alert-success" role="alert">
            {{ SuccessMsg }}
        </div>
    {% endif %}
    {% if ErrorMsg != '' %}
        <div class="alert alert-danger" role="alert">
            {{ ErrorMsg }}
        </div>
    {% endif %}
</div>
{% endblock %}
